config:
  pulsar.spout.topic: "persistent://public/crawl/in"
  # use the topology id as subscription name by default
  # pulsar.spout.subscription: "sc-subscription"

  pulsar.status.topic: "persistent://public/crawl/out"
  # use the topology id as producer name by default
  # pulsar.status.producerName: "stormcrawler"
  pulsar.status.sendTimeout: 0